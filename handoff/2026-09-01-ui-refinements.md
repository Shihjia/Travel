# 資訊呈現調整：天氣移到右上角圖示＋飯店/班機連結改圖示＋飯店名稱統一

- 狀態：✅ 完成（2026-09-02 Claude Code 實作完畢，待驗收）
- 建立日期：2026-09-01
- 提出者：Claude（依使用者需求整理）

## 需求背景

使用者看過線上版本後提出的呈現方式調整：逐日行程頁的天氣資訊佔掉一整塊版面，希望改成右上角的小圖示；總覽頁的飯店／班機卡片裡，連結都是文字列，希望改成圖示放在名稱旁邊，並把「查看設施」提到最前面。另外順便解決上一份規格驗收時發現的飯店名稱用詞不一致問題。

## 與其他規格的執行順序

目前 `handoff/` 還有兩份未執行的規格：`2026-09-01-stop-photos.md`（景點照片）與 `2026-09-01-shopping-guide.md`（採買推薦頁）。

**建議執行順序：先做這一份，再做那兩份。** 原因是這份會動到 `script.js` 的 `loadWeather()` 與 day 頁面的 hero 結構，而景點照片那份會動到 `renderTimeline()` 與 day 頁面的時間軸；先把 hero／天氣定下來，照片那份就不會踩到同一塊。三份彼此沒有資料衝突，但分開 commit 比較好追。

## 影響範圍

- 頁面：`2026-okinawa/day1.html` ~ `day6.html`（天氣位置）、`2026-okinawa/index.html`（飯店／班機卡）
- 邏輯：`2026-okinawa/script.js`（`loadWeather()` 新增顯示模式）
- 資料：`2026-okinawa/data/itinerary.js`（`hotels` 新增 `shortName`）
- 樣式：`assets/style.css`

---

## 需求 1：逐日行程頁的天氣改成右上角圖示

**現況**：day 頁面的 `<div class="card weather-panel" id="weather-box">` 是 `<main>` 裡最上面的一整塊卡片，佔掉不少版面；hero 右上角則是一個純裝飾用、沒有意義的 `☀️`（`.hero__sun`）。

**要求**：把天氣搬到 **hero 的右上角**，取代那個裝飾用的太陽，`<main>` 裡原本的整塊天氣卡片移除。

顯示內容（三個資訊都要有）：
1. **天氣圖示** — 依 WMO weathercode 對應晴天／多雲／陰天／雨天等（`script.js` 既有的 `WEATHER_ICONS` 對照表已經涵蓋，沿用即可，維持 emoji 呈現，與全站風格一致）
2. **溫度** — 最低～最高，例如 `24° - 28°`
3. **降雨機率** — 例如 `☔ 30%`

**尚未進入預報範圍時（重要，目前 6 天全部都是這個狀態）**：
- 使用**統一的圖示**（建議 `🗓️`，或一個淡化的天氣圖示，6 天全部一致），不要用會被誤認成「當天是晴天」的 `☀️`／`🌤️`。
- 搭配統一說明文字，內容意思要是「還查不到，出發前再看」，例如：`預報未開放` ＋ 更小的一行 `出發前約 2 週開放查詢`。文案你可以微調得更順，但**六天必須一致**。
- 現在（2026-09-01）距離出發還有一個半月，所以你在本機看到的一定是這個狀態；這是正確行為不是 bug。10 月上旬之後會自動變成實際預報。

版面要求：
- 位置：`.hero` 內絕對定位於右上角，與 `.hero__back`（左上角返回連結）同一水平帶但靠右，不可重疊。
- 外觀：半透明白底的小圓角卡片（例如 `rgba(255,255,255,0.22)` ＋ `backdrop-filter: blur`），文字白色，在藍色漸層 hero 上要清楚可讀。
- **手機 375px**：hero 寬度變窄，天氣區塊不可與標題（`.hero__title`，例如「抵達日：那霸機場 → 波上宮 →…」）重疊或把標題擠變形。可以縮小字級、改成更精簡的排法（例如圖示與溫度同一行、降雨機率換行），必要時允許在手機改放在標題下方，但**六天要一致**。
- 六個 day 頁面都要套用，寫法一致。

實作建議：
- `script.js` 的 `loadWeather(containerId, lat, lon, dateStr)` 新增第 5 個參數 `options`，用 `options.variant === "badge"` 走新的右上角版型；**不傳 options 時維持現有行為**，這樣總覽頁 `index.html` 逐日卡片裡的小天氣（`.day-card__weather`）完全不用改、也不會壞。
- 「尚未進入預報範圍」與「抓取失敗」目前共用 `weatherPendingHtml()`，badge 版型也要有對應的精簡版。

**不要動**：總覽頁逐日卡片上的小天氣維持現狀。

---

## 需求 2：飯店／班機卡片的連結改成圖示

### 2-1　飯店卡（總覽頁「🏨 住宿資訊」）

**現況**（`index.html` 第 131 行附近）：展開後的最後一行是三個文字連結並排 —— `飯店官網 →`、`Google 地圖 →`、`查看設施／餐廳詳情 →`。

**改成**：

- **卡片標題列**：飯店名稱右邊緊接著兩個圖示連結
  - 🌐 飯店官網（`h.website`）
  - 📍 Google 地圖（`h.mapUrl`）
  - 兩者都 `target="_blank" rel="noopener"`，並加上 `aria-label` 與 `title`（例如 `title="飯店官網"`），滑鼠移上去看得到說明。
  - **⚠️ 實作重點**：`.info-card__head` 整塊是可點擊的展開／收合觸發區，圖示連結必須 `e.stopPropagation()`，否則點官網會順便把卡片收起來。請實際點測。
  - 點擊區域至少 32×32px（手機好按）。
- **展開後的內容**：**「查看設施／餐廳詳情 →」移到第一列**，放在「房型」之前，並且做成明顯一點的樣式（例如 `.timeline-link` 那種膠囊按鈕，而不是純文字），因為這是使用者最想點的東西。
- 展開內容裡**移除**原本的官網／地圖文字連結（已經移到標題列了）。
- 順便把展開內容裡「**設施：**含早餐・私人停車位…」這一列的標籤改成「**房價包含：**」。原本叫「設施」會跟「查看設施詳情」混淆，那一行其實講的是房價含什麼。

### 2-2　班機卡（總覽頁「✈️ 班機資訊」）

- **卡片標題列**：航班名稱（例如「去程・長榮航空 BR112」）右邊加一個 🌐 圖示連結到 `f.website`，規格同上（新分頁、aria-label／title、stopPropagation、32×32px 點擊區）。
- 展開內容裡**移除**原本最後一行的「前往航空公司官網 →」文字連結。
- 其他欄位（機型／起飛／抵達／行李／提醒）維持不變。

---

## 需求 3：飯店名稱用詞 —— ✅ 已完成，不用再動

原本這一項要求新增 `shortName` 欄位、時間軸與打包清單改用簡稱。**後來使用者直接指示 Claude Code 統一使用完整名稱「沖繩嘉利吉海灘海洋溫泉度假村」**，已於 commit `6636c78` 完成（全站 11 處一致）。

**以使用者的決定為準：全站一律使用完整名稱，不需要 `shortName` 欄位，這一項不用做任何事。**

（先前 Claude 在驗收其他規格時誤把這個改動判讀成「偏離規格」，是 Claude 的誤判，已更正。）

## 驗收標準

- [ ] 6 個 day 頁面的天氣都顯示在 hero 右上角，`<main>` 裡原本的整塊 `weather-panel` 已移除
- [ ] 天氣區塊同時顯示「圖示＋溫度＋降雨機率」三項資訊（有預報時）
- [ ] 尚未進入預報範圍時，六天顯示**同一個**圖示與同一段說明文字，且該圖示不會被誤認為晴天
- [ ] 天氣區塊不與 `.hero__back`、`.hero__title` 重疊；桌機 1280px 與手機 375px 都要檢查
- [ ] 總覽頁逐日卡片的小天氣**維持原樣沒有被改壞**（`loadWeather` 舊呼叫方式仍正常）
- [ ] 飯店卡標題列有 🌐 官網、📍 地圖兩個圖示連結，點擊會開新分頁，**且不會把卡片收合**
- [ ] 飯店卡展開後第一列是「查看設施／餐廳詳情」，且樣式比純文字明顯
- [ ] 飯店卡展開內容已無官網／地圖文字連結，「設施：」標籤已改為「房價包含：」
- [ ] 班機卡標題列有 🌐 官網圖示連結，行為同上；展開內容已無官網文字連結
- [ ] 圖示連結都有 `title` / `aria-label`，點擊區域 ≥ 32×32px
- [ ] 全站 Console 無錯誤，手機 375px 無橫向溢出

## 不在範圍內

- 不用改總覽頁逐日卡片的小天氣呈現
- 不用改 `hotel1.html` / `hotel2.html` 詳情頁的內容結構
- 不用換掉 emoji 改用 SVG 圖示（維持全站 emoji 風格）
- 景點照片、採買推薦頁是另外兩份規格，不在這份範圍
- header 插圖是另一份規格（`2026-09-01-hero-illustration.md`），等使用者提供圖片後才會啟動，這次不用預留

---

## Claude Code 實作回報（由 Claude Code 填寫）

- 異動檔案清單：
  - `2026-okinawa/script.js`（**修改**：`loadWeather()` 新增第 5 個參數 `options`；新增 `weatherBadgePendingHtml()`；`initInfoCardToggles()` 加上連結防呆）
  - `2026-okinawa/day1.html` ~ `day6.html`（**修改**：各 4 處 —— 移除 `.hero__sun`、hero 加 `hero--weather`、標題後加天氣容器、移除 `<main>` 的 `weather-panel`、`loadWeather` 改帶 `{ variant: "badge" }`）
  - `2026-okinawa/index.html`（**修改**：班機卡／飯店卡標題列圖示連結、展開內容調整）
  - `assets/style.css`（**修改**：新增 `.hero__weather` / `.weather-badge__*` / `.icon-link`，`.info-card__head-title` 改為 flex）
  - `handoff/2026-09-01-ui-refinements.md`（本回報）
  - 需求 3 依規格說明**不做任何事**，`itinerary.js` 完全沒有動（沒有新增 `shortName`）。

- 做了什麼：
  - **需求 1（天氣移到 hero 右上角）**
    - `loadWeather(containerId, lat, lon, dateStr, options)` 新增第 5 個參數。`options.variant === "badge"` 才走新版型，**不傳 options 時完全維持原本的行為與 HTML**，所以總覽頁逐日卡片的小天氣一行都沒改。
    - 有預報時 badge 顯示三項：`weatherIcon(code)` 的 emoji ＋ `24° - 28°` ＋ `☔ 30%`（沿用既有 `WEATHER_ICONS` 對照表）。
    - **尚未進入預報範圍**：新增 `weatherBadgePendingHtml()`，六天統一顯示 **🗓️ ＋「預報未開放」＋「出發前約 2 週開放查詢」**。刻意不用 ☀️／🌤️，避免被誤看成當天是晴天。日期已過的情況也有對應版本（🗓️ ＋「已無預報」＋「此日期已過」）。
    - 六個 day 頁面用同一支腳本套用，寫法完全一致（見測試第 5 點的逐頁比對）。
    - 天氣容器加了 `role="img"` 與 `aria-label`；有實際預報時 `aria-label` 會被更新成「當日預報 24 到 28 度，降雨機率 30%」，讀螢幕軟體不會只讀到一串符號。
  - **需求 2（連結改圖示）**
    - 飯店卡標題列：名稱後面接 🌐（`h.website`）與 📍（`h.mapUrl`）；班機卡標題列：航班名稱後面接 🌐（`f.website`）。全部 `target="_blank" rel="noopener"`，並帶 `title` 與 `aria-label`（例如 `title="Google 地圖"`、`aria-label="在 Google 地圖開啟沖繩嘉利吉海灘海洋溫泉度假村（開新分頁）"`）。
    - 新增 `.icon-link`：**34×34px** 圓形（超過規格要求的 32×32），淺色底＋細框，hover 變黃。
    - 展開內容：**「查看設施／餐廳詳情 →」移到第一列**（在「房型」之前），用 `.timeline-link` 膠囊樣式＋珊瑚紅配色，比純文字明顯很多。
    - 展開內容已移除飯店官網／Google 地圖／航空公司官網三個文字連結。
    - 「**設施：**」標籤改成「**房價包含：**」。

- 是否有偏離原規格（及原因）：
  1. **防止「點官網順便收合」的作法**：規格寫的是「圖示連結必須 `e.stopPropagation()`」。我改成在 `initInfoCardToggles()` 的收合處理裡加一行 `if (e.target.closest("a")) return;`。效果相同，但有兩個好處：(a) 邏輯集中在一個地方，不必在每個用 `innerHTML` 產生的連結上掛 handler 或寫 inline `onclick`；(b) 以後標題列再加任何連結都自動免疫，不會有人忘記加而踩到同一個雷。實際點測確認卡片不會收合（見測試第 3 點）。如果你堅持要用 `stopPropagation()` 我可以改回來。
  2. **天氣在手機不是「右上角」而是「標題下方」**：規格允許「必要時允許在手機改放在標題下方」。Day1 的標題在 375px 下會佔滿三行、幾乎撐滿整個 hero，右上角絕對定位一定會壓到標題。所以桌機（≥640px）絕對定位在右上角、手機（<640px）落在標題正下方的一般流位置。**六天寫法完全一致**。為了讓手機版自然落在標題下方，天氣容器在 DOM 裡放在 `<h1>` 之後（而不是原本 `.hero__sun` 的位置），桌機再用 `position:absolute` 拉到右上角。
  3. **溫度格式寫成 `24° - 28°` 而不是 `24° - 28°C`**：badge 空間小，拿掉 C 讓字不會擠。總覽頁小天氣的舊版型仍維持 `°C` 不變。

- 怎麼測試的（含手機 375px 下天氣與標題的實測結果）：
  1. **桌機 1280px（day1，標題最長的一天）**：實測座標 —— 天氣 `top:20 right:1243 left:1056`（右上角）、`.hero__back` `top:56 right:103`、`.day-badge` `top:32 right:322`、標題 `top:95 bottom:138`。程式化計算三組矩形交集：**`overlapBack: false`、`overlapDayBadge: false`、`overlapTitle: false`**，完全沒有重疊。天氣區塊垂直落在 20~75，標題從 95 才開始，中間還有 20px 淨空。
  2. **手機 375px（day1）**：`getComputedStyle().position === "static"`（確認有正確退回一般流），天氣位於 `top:236`、標題 `top:95 bottom:224` 佔 **3 行**且寬度完整 331px（**沒有被擠壓變形**），天氣在標題結束後 12px 處。三組重疊判定同樣**全部 false**。`document.scrollWidth === 375`，**無橫向溢出**。
  3. **圖示連結不會收合卡片（規格特別點名的雷）**：兩種方式都測了 ——
     (a) 攔截導航但讓事件照常冒泡，連續點飯店卡的 🌐 與 📍、班機卡的 🌐，卡片 `is-open` 前後都是 `true`，**沒有被收起來**；接著點標題文字本身，卡片正常切換（確認收合功能本身沒被我改壞）。
     (b) **真實點擊**飯店2 的 🌐（未攔截）：目前分頁 URL 沒變（仍在 `index.html`）、卡片仍是展開狀態、展開內容高度 > 0。
  4. **圖示規格**：6 個 `.icon-link` 實測全部 **34×34px**；`title` 依序為「飯店官網／Google 地圖／長榮航空官網…」；全部具備 `aria-label`、`target="_blank"`、`rel` 含 `noopener`。
  5. **六天一致性**：`grep` 逐頁比對，day1~day6 都是 `hero--weather` ×1、`hero__weather` 容器 ×1、`weather-panel` **0 個**、`hero__sun` **0 個**、`variant: "badge"` ×1。實際開 day1／day3／day4／day6 四頁，天氣文字**全部都是**「🗓️ 預報未開放 出發前約 2 週開放查詢」。
  6. **總覽頁小天氣沒被改壞**（規格明列的不可回歸項）：`index.html` 的 6 張逐日卡片實測仍是舊版型（`.weather__icon` ×6，內容仍是「🌤️ 2026-10-20 天氣預報通常只能提前約 10-16 天查詢…」），確認 4 參數的舊呼叫方式照常運作。
  7. **展開內容結構**：飯店卡第一列確實是 `class="timeline-link"` 的「查看設施／餐廳詳情 →」（href 分別為 `hotel1.html`／`hotel2.html`），第二列「房型：」、第三列「**房價包含：**」；全域確認 `<strong>設施：</strong>` 已不存在；展開區內剩下的連結只有那兩個詳情連結，官網／地圖文字連結已清乾淨。班機卡展開內容剩機型／起飛／抵達／行李／提醒五列，官網那列已移除。
  8. **手機 375px 的卡片標題列**：6 個圖示仍是 34×34、全部在容器範圍內、`scrollWidth === 375` 無溢出。兩個較長的飯店名稱會換行讓圖示掉到第二行（`flex-wrap` 的預期行為），沒有擠壓或溢出。
  9. **回歸**：index、day4、hotel1、shopping、packing 全部重開，**Console 皆無錯誤**。

- 待確認／已知問題：
  1. **新分頁我沒辦法在本機測試環境百分之百證實**。預覽用的內嵌瀏覽器似乎會擋掉 `target="_blank"` 開新分頁（點下去 `tabs_context` 只看得到原本那一個分頁）。不過 DOM 屬性（`target="_blank"` + `rel="noopener"`）已逐一驗證正確，而且真實點擊後**目前分頁沒有被導走**，在一般瀏覽器就是開新分頁的標準行為。實際部署後你點一下就能確認。
  2. **10 月上旬之後天氣會自動變成真實預報**，屆時 badge 會變成「圖示＋`24° - 28°`＋`☔ 30%`」。那條路徑我在本機無法觸發（日期還沒到），是照既有 API 回傳欄位寫的，與原本卡片版型取用同樣的 `weathercode` / `temperature_2m_max` / `temperature_2m_min` / `precipitation_probability_max`。**若你要提早驗證**，可以暫時把某天的 `weatherAnchor` 日期改成近期日期測一次，我可以配合做。
  3. **瀏覽器快取**：我在本機測試時被 `script.js` 的快取騙過一次（HTML 更新了但 JS 還是舊的）。GitHub Pages 有自己的快取標頭，你如果部署後看到天氣還是舊版面，請先強制重新整理（Ctrl+F5）再判斷。

---

## Claude 驗收結果（由 Claude 核對後填寫）

- 逐項驗收標準檢查結果：
- 需要調整的地方：
- 資料是否已同步回 itinerary.js／規劃 markdown 主檔：
